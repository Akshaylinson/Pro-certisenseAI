# Hardhat local node RPC
RPC_URL=http://127.0.0.1:8545

# Private key of account to sign transactions (for local dev only)
# Example: use one of the private keys printed by 'npx hardhat node'
PRIVATE_KEY=0xaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

# Contract address after deploy (replace after running deploy)
CONTRACT_ADDRESS=0x...
